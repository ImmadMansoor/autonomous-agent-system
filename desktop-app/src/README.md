# Legacy UI Backup

This folder preserves the pre-Nothing/Stitch-inspired MenuMind frontend source.
It is not imported by the app and exists only as a rollback/reference copy.

The active frontend lives in `frontend/src`.
