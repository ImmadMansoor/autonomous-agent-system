MenuMind Project History A to Z
Last updated: 2026-05-21
Owner/user: Tayyab
Assistant name in shared docs: Carl

Purpose
- Durable handoff for any AI/teammate joining MenuMind with no prior context.
- Keep under 1000 lines; do not paste chat transcripts.
- Include only what matters for architecture, demo, deployment, and next work.

1. Current Product
- MenuMind is an autonomous cafe operations agent for the Google Antigravity Hackathon, Challenge 1: content-to-action agentic workflow development.
- Core loop: messy signal -> Gemini reasoning -> policy guardrail -> tool execution -> database mutation -> trace -> UI explanation.
- The runtime agent lives in this app, mainly `backend/agent.py`, `backend/llm_adapter.py`, and `backend/tools.py`.
- Google Antigravity was used as the development environment, not as the production agent runtime.

2. Current Stack
- Backend: FastAPI, SQLite, SQLAlchemy, Pydantic.
- Agent: Gemini REST planner with deterministic safety fallback.
- Model routing: settings-driven Gemini model selection.
- Frontend: Next.js app under `frontend/src/app`.
- Live frontend: Vercel, `https://menumind-nu.vercel.app`.
- Live backend: Render, `https://menumind-backend.onrender.com`.
- Desktop: Tauri build exists locally.
- Mobile: Capacitor files exist, but APK workflow still needs review because active frontend is Next.js, while older docs/scripts mention Vite.

3. Core Backend Files
- `backend/main.py`: API routes, auth, settings, analytics, audit, health.
- `backend/agent.py`: observe/reason/policy/execute/finalize pipeline.
- `backend/llm_adapter.py`: Gemini planner, dynamic model routing, safety fallback.
- `backend/tools.py`: tool registry, state mutations, notifications, Make webhook dispatch.
- `backend/models.py`: SQLAlchemy tables.
- `backend/schemas.py`: Pydantic contracts.
- `backend/weather.py`: no-key Open-Meteo weather context.

4. Core Frontend Files
- `frontend/src/app`: active Next.js routes.
- `frontend/src/app/planner/page.tsx`: live Planner / Ops Board.
- `frontend/src/app/analytics/page.tsx`: Intelligence page with Business Impact panel.
- `frontend/src/components/analytics/BusinessImpact.tsx`: financial impact forecaster.
- `frontend/src/components/settings/AIPreferences.tsx`: AI settings, threshold, decision speed UI.
- `frontend/src/lib/api.ts`: frontend API adapter.
- `frontend/src/lib/auth.tsx`: frontend auth state and demo token handling.
- `frontend/src/lib/agentView.js`: deterministic frontend view helpers over real agent output.

5. Base Autonomous Agent
- Built signal ingestion through `POST /signals`.
- Built agent execution through `POST /agent/run/{signal_id}`.
- Agent produces structured final decisions, workplans, recommendations, simulated execution, demand forecast, revenue projection, strategic advice, and extracted facts.
- Tool registry supports safe cafe operations such as menu availability, promotion, staff alerts, customer notifications, approvals, and audit/trace logging.
- High-risk actions are paused through approval guardrails instead of being blindly executed.
- Crisis or price-gouging scenarios should create approvals instead of unsafe automatic surge pricing.

6. Gemini Reliability
- Gemini REST path works with current Gemini model names.
- Timeout protection and safety fallback remain in place.
- Backend `/health` reports planner mode, default model, and timeout.
- Safety fallback is intentionally preserved for demo resilience if network/API stalls.

7. Decision Speed Model Routing
- Implemented real backend model routing from Settings -> AI Agent Preferences -> Decision Speed.
- Fast routes to `gemini-2.5-flash` with lower latency.
- Balanced routes to `gemini-2.5-pro` with deeper reasoning.
- Thorough routes to `gemini-3-flash-preview` as the higher-depth preview path.
- Backend reads saved preferences for the current user during agent runs and passes selected speed into `GeminiPlanner`.
- This is real backend logic, not only frontend labels.
- Environment overrides exist: `GEMINI_FAST_MODEL`, `GEMINI_BALANCED_MODEL`, `GEMINI_THOROUGH_MODEL`, plus matching timeout variables.

8. Decision Speed Validation
- Python compile passed for model-routing backend files.
- Mocked Gemini endpoint test proved each speed calls its matching model endpoint and timeout.
- Live Vercel bundle contains the model labels.
- Live backend settings endpoint returns normalized settings.
- A live production run was not triggered for model switching because it would mutate live demo data and use Gemini quota.

9. Auto-Approve Threshold Fix
- Fixed old `0.7%` display bug.
- Backend default is now `70`.
- Backend normalizes legacy `0.7` values to `70`.
- Frontend also normalizes old values.
- UI now displays `70%` instead of `0.7%`.

10. Logo / App Icon Centering
- Used finalized MenuMind logo reference to regenerate centered icon assets for desktop.
- Star mark is visually centered in the app icon.
- Tauri icon assets were regenerated locally.
- Desktop portable build was produced locally under `Windows Application/`.
- Do not commit generated binaries unless user explicitly asks.

11. Planner / Ops Board
- Antigravity built the Planner concept; Carl reviewed, fixed, committed, pushed, and verified it live.
- Live route: `https://menumind-nu.vercel.app/planner/`.
- Pulls latest run, trace, approvals, notifications, menu diff, and matching signal text through real API calls.
- Builds operational task cards from actual agent output via `buildOpsPlan`.
- Columns: Now, Next, Needs Approval, Done.
- Task cards include owner, due timing, priority, and confidence.
- Completed tasks persist in `localStorage`.
- Completion state is scoped by `run_id`, so old completed tasks do not incorrectly mark future run tasks as done.
- Empty state lets demo users ingest a custom signal or apply preset signals.
- Important truth: Planner is data-driven from real agent runs, but has safe empty/demo states when no backend run exists.

12. Business Impact Analytics
- Antigravity added Option 2: Dynamic Margin & Financial Impact Forecaster; Carl verified and pushed it live as part of the Planner release.
- Location: `https://menumind-nu.vercel.app/analytics/`.
- Polls latest agent run every 8 seconds.
- Shows Projected Profit, Prevented Stock Loss, Guardrail Events, and Menu Changes Executed.
- Shows intervention flow: Input Signal -> Agent Decision -> Business Value -> Safety Guardrail.
- Uses real run fields when present.
- Uses safe demo fallbacks only when no latest run or no revenue/demand fields exist.
- Revenue values are parsed safely as numbers to avoid render crashes from string API values.

13. Deployment History
- Commit `6ab52f7`: wired Decision Speed to Gemini model routing.
- Commit `b1cc537`: added Planner and Business Impact analytics.
- Commit `caf50ec`: initially added compact markdown project history, but user clarified the durable file should be this `.txt` file.
- Vercel production deployment for Planner/Business Impact reached READY.
- Verified live: `/planner/` returns HTTP 200.
- Verified live: `/analytics/` returns HTTP 200.
- Verified live backend endpoints used by Planner return HTTP 200.

14. Notion / External Repo Status
- No real Notion integration is currently added.
- The repo has references to "Notion-style" UI wording only.
- There is no Notion API auth, sync, database creation, or page publishing logic.
- External uploaded repos or duplicate source dumps should not be copied into the app unless there is a specific, small, justified integration.
- Most external repos are too risky/noisy for the hackathon deadline.

15. Known Remaining Gaps
- Live backend may return no runs for `demo-token` after deploy/reset. Planner then shows empty state until a signal is ingested.
- `npm run lint` is stale for the current Next.js version because `next lint` is no longer valid in this setup.
- Use `npm run build` and `npx tsc --noEmit` for now until lint script is replaced.
- Mobile/APK story needs cleanup. Existing workflow references Vite-style `VITE_API_URL`, while active app uses `NEXT_PUBLIC_API_URL`.
- Some old docs still contain Vite-era wording. Prefer this file plus live code when there is a conflict.
- Local untracked artifacts should not be blindly committed: duplicate repo folders, Windows application binaries, handoff docs, and old zip deletion.

16. Recommended Next Work
- Stop adding large features unless judges explicitly need one.
- Polish live demo path: Settings -> Decision Speed, Operations signal run, Planner board, Analytics Business Impact, Logs trace, Approvals guardrail.
- Fix mobile/APK documentation and workflow if a mobile deliverable is required.
- Clean stale docs and remove/ignore duplicate local dumps carefully.
- Optionally add a small demo seed button/script to create one safe backend run before judging, but do not hide that it is demo data.

17. Validation Commands Used Recently
- Frontend build:
  cd frontend
  npm run build
- Frontend typecheck:
  cd frontend
  npx tsc --noEmit
- Backend compile:
  python -m py_compile backend/main.py backend/agent.py backend/llm_adapter.py
- Live checks:
  Vercel deployment status via Vercel connector.
  Live route fetches for `/planner/` and `/analytics/`.
  Render backend endpoint checks for `/agent/runs?limit=1` and `/signals?limit=10`.

18. Rules For Future AI Sessions
- Start with `AGENTS.md`, then this file, then `SHARED_PLAN.md`.
- Do not read `SHARED_BRAINSTORMING.md`, PDFs, logs, `node_modules`, `.venv`, `.next`, database files, or generated binaries unless specifically needed.
- Do not use `git add .` in this repo because unrelated local artifacts often exist.
- Keep changes scoped and verified.
- Preserve the signal -> plan -> tools -> database -> trace -> UI loop.
- Do not remove safety fallback.
- Do not present demo fallbacks as live autonomous reasoning.
